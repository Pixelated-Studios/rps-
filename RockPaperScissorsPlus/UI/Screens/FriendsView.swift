// FriendsView.swift
import SwiftUI

struct FriendsView: View {
    var body: some View {
        GlassCard {
            Text("friends placeholder")
                .font(.title)
                .foregroundColor(.primary)
        }
        .navigationTitle("friends")
    }
}
