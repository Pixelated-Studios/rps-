// SettingsView.swift
import SwiftUI

struct SettingsView: View {
    var body: some View {
        GlassCard {
            Text("settings placeholder")
                .font(.title)
                .foregroundColor(.primary)
        }
        .navigationTitle("settings")
    }
}
